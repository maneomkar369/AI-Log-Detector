[pytest]
# Keep default discovery focused on real unit/integration tests.
testpaths =
    edge_server/tests
python_files = test_*.py
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra
